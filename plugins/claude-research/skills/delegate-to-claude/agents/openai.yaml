interface:
  display_name: "Delegate to Claude"
  short_description: "Discuss, implement, audit, and interpret with Opus"
  default_prompt: "Use $delegate-to-claude to discuss and agree on a trustworthy experiment before any Opus implementation or execution."
dependencies:
  tools:
    - type: "mcp"
      value: "claude_research"
      description: "Local Claude Code executor and research auditors"
policy:
  allow_implicit_invocation: true

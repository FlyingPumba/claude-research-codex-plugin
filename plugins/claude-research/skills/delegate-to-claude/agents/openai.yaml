interface:
  display_name: "Delegate to Claude"
  short_description: "Implement and audit research experiments with Opus"
  default_prompt: "Use $delegate-to-claude to implement and audit this agreed research experiment, with separate approval before its full run."
dependencies:
  tools:
    - type: "mcp"
      value: "claude_research"
      description: "Local Claude Code executor and research auditors"
policy:
  allow_implicit_invocation: true
